# WordPress Headless CMS
# The REST API is public — no authentication token required for published posts.
# WP_API_BASE is already hardcoded in src/lib/wordpress.ts as https://cms.virtusoperandi.com/wp-json/wp/v2
# No environment variables are required for the build.
